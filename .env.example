# Optional for this public dataset. If Kaggle requests authentication, create an
# API token at https://www.kaggle.com/settings and paste it after the equals sign.
KAGGLE_API_TOKEN=

# Optional local API configuration. These defaults are already built into the
# application; uncomment only when you need to override them.
# TALENT_MODEL_URI=models:/talent-attrition-classifier@candidate
# MLFLOW_TRACKING_URI=sqlite:////absolute/path/to/repository/.mlflow/mlflow.db
# TALENT_MODEL_DESCRIPTOR=/absolute/path/to/model-descriptor.json
# TALENT_API_HOST=127.0.0.1
# PORT=8000
